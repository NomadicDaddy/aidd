## Which files define the benchmark score inputs and final task weighting?

Explain where the score inputs come from, how task categories are represented, and which helper computes the final weighted score.
