# Project structure

- `backend/src/server.ts` holds the HTTP handlers: the debug page, the administrator check, and
  the health endpoint.
- `backend/src/storage.ts` reads uploaded files and builds public asset URLs.
