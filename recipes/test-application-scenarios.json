{
	"description": "Run the scripted scenarios in .aidd/testing-scenarios.md via the tester skill, then convert bugs to features.",
	"name": "test application (scenarios)",
	"parameters": [
		{
			"description": "Target application name",
			"name": "application"
		}
	],
	"steps": [
		{
			"configJson": {
				"args": "{application} --scenarios .aidd/testing-scenarios.md: run every scenario in the project catalog, in order; if the catalog is missing or has no scenarios, report that and stop rather than substituting an exploratory pass",
				"executionIntent": "apply-changes",
				"skillId": "tester"
			},
			"name": "Run testing scenarios",
			"stepType": "skill"
		},
		{
			"configJson": {
				"params": {
					"application": "{application}"
				},
				"recipeName": "bug2feature"
			},
			"name": "Bug to feature",
			"stepType": "recipe-ref"
		}
	]
}
