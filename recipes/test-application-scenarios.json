{
	"description": "Run testing scenarios from testing-scenarios.md, then convert bugs to features.",
	"name": "test application (scenarios)",
	"parameters": [
		{
			"description": "Target application name",
			"name": "application"
		}
	],
	"steps": [
		{
			"configJson": {
				"args": "{application} run the scripted scenarios recorded in .aidd/testing-scenarios.md",
				"executionIntent": "apply-changes",
				"skillId": "spernakit-tester"
			},
			"name": "Run testing scenarios",
			"stepType": "skill"
		},
		{
			"configJson": {
				"params": {
					"application": "{application}"
				},
				"recipeName": "bug2feature"
			},
			"name": "Bug to feature",
			"stepType": "recipe-ref"
		}
	]
}
