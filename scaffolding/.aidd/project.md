This project's architecture, tech stack, style, and tooling are defined by the codebase itself and by `.aidd/project-profile.json` (when present); treat that inferred profile as the authoritative record of the stack. Do not impose a different language, framework, or toolchain on an existing codebase; match and extend the conventions already in place.

Uphold the project's own quality gates, documentation, and commit hygiene. When a stack, tooling, or convention question is genuinely unresolved, defer to the project profile and the surrounding code rather than introducing new preferences.
